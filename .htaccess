RewriteEngine On

# Se o arquivo ou pasta existir na raiz, serve normalmente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redireciona tudo para o public/index.php
RewriteRule ^(.*)$ /public/index.php?url=$1 [QSA,L]
